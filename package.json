{
  "name": "qwen-coding-agent",
  "version": "1.0.0",
  "description": "Production-grade CLI coding agent powered by Qwen LLM",
  "type": "module",
  "bin": {
    "qwen-agent": "./dist/agent/cli.js",
    "agent": "./dist/agent/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.qwen.json",
    "start": "node dist/agent/cli.js",
    "dev": "tsx agent/cli.ts",
    "dev:repl": "tsx entrypoints/qwen-agent.ts",
    "lint": "tsc -p tsconfig.qwen.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "clean": "rm -rf dist",
    "setup": "npm install && cd desktop && npm install",
    "postinstall:desktop": "cd desktop && npm install",
    "desktop": "cd desktop && npm run dev",
    "desktop:dev": "cd desktop && npm install && npm run dev",
    "desktop:build": "cd desktop && npm install && npm run build",
    "desktop:preview": "cd desktop && npm install && npm run preview",
    "verify": "npm run build && cd desktop && npm install && npm run build"
  },
  "dependencies": {
    "commander": "^12.1.0",
    "proper-lockfile": "^4.1.2"
  },
  "devDependencies": {
    "@types/node": "^22.19.17",
    "@types/proper-lockfile": "^4.1.4",
    "tsx": "^4.19.4",
    "typescript": "^5.9.3",
    "vitest": "^2.0.0"
  }
}
